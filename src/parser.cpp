#include <combinator.h>
#include <parser.h>

#include <span>

namespace solver
{

// auto ParseFunction(std::span<Token> state) -> Parser<Expression>
// {

// }

// auto Parse(std::vector<Token> const& tokens) -> std::unique_ptr<Expression>
// {
//     auto state = std::span<Token>(tokens);
//     auto 
// }

} // namespace solver
